name: Deploy Vue to GitHub Pages

on:
  push:
    branches:
      - main # Ana dalınızın adı 'main' değilse ('master' ise) burayı güncelleyin

permissions:
  contents: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v4

      - name: Deploy 🚀
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: BarnManagement.API/wwwroot
          branch: gh-pages
